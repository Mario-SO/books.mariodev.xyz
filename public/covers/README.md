# Book covers

Add local cover images to this directory, then set a book's `cover` value in
`src/data/books.json` to its root-relative path, for example
`/covers/the-quiet-archive.webp`. An empty value uses the built-in CSS cover.
